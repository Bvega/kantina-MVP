This directory is reserved for reusable UI components.  Components that
are shared across multiple pages (e.g. form inputs, modals, navigation
bars) should be defined here.  Currently, the MVP does not require any
shared components beyond what is defined in the pages.